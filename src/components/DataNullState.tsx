import { Typography } from '@mui/material';
import { FC } from 'react';

export const DataNullState: FC = () => (
  <Typography color="text.secondary" variant="caption">
    No data available
  </Typography>
);
